# CLIP requirements for Disco Diffusion
# Install with: pip install -r requirements_clip.txt

# Core CLIP dependency
clip-by-openai @ git+https://github.com/openai/CLIP.git

# CLIP dependencies (if not already installed)
torch>=1.7.1
torchvision>=0.8.2
ftfy
regex
tqdm